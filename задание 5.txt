1) 
UPDATE profiles SET
	created_at = NOW()
	WHERE  created_at = NULL ;

UPDATE profiles SET
	updated_at = NOW()
	WHERE  updated_at = NULL ;

2)
UPDATE new_table SET
	new_colomn = STR_TO_DATE( '20.10.2017 8:10', '%d.%m.%y %H:%i')

3)
SELECT * ,
'value' = 0
FROM storehouses_products
ORDER BY 'value' = 0, 'value'

Агрегация данных:

1)
SELECT 
	AVG  (timestampdiff(year,birthday, NOW())) AS 
    ageAvg FROM profiles;

2)
SELECT
	birthday,
	dayname(CONCAT(YEAR(NOW()), RIGHT( birthday, 6)))
    FROM profiles;


